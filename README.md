# money-tracker
gacha-inspired savings-tracker web app <br>
! still a in prototyping phase

# project setup
1. start frontend <br>
`cd frontend` <br>
`npm install` <br>
`npm run dev` <br>
2. start backend (in another terminal) <br>
`cd backend` <br>
`npm install` <br>
`npm run start`<br>
